[project]
name = "eh-skills"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "claude-agent-sdk>=0.1.48",
    "pyright>=1.1.408",
    "ruff>=0.15.5",
]

[tool.uv.workspace]
members = [
    "dashboard",
]
